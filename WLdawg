local whitelisted = ("Teebowzz") -- perm
local whitelisted2 = ("Swagykid9876") -- perm


 
function script()
print("Lancement...")
 
end
 
local nonamexd = game.Players.LocalPlayer.Name
if nonamexd == whitelisted or nonamexd == whitelisted2 then
wait(1)
script()
warn("Whitelist réussie !")
loadstring(game:HttpGet('https://raw.githubusercontent.com/Homsat21/623wgy777qsad/main/maintypashizl0ol'))()
else
wait(2)
game.Players.LocalPlayer:kick "Compte pas whitelist. Veuillez contacter Pomaz0o#0026 sur Discord concernant ce problème."
end
